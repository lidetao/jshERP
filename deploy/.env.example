# =============================================
# jshERP 敏感配置模板
# 复制为 .env 并填入真实值
# cp .env.example .env && chmod 600 .env
# =============================================

# 阿里云 RDS MySQL
DB_HOST=<RDS内网地址>
DB_USERNAME=<RDS账号>
DB_PASSWORD=<RDS密码>

# Redis
REDIS_PASSWORD=<Redis密码>
